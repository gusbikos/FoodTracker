"""
SQLite or PostgreSQL for storing meals/users

Purpose:
Handles persistent storage.

Responsibilities:

Database connection setup

Session management

Migrations

Query logic

Examples:

SQLite for local development

PostgreSQL for production

Think of it as:

“Where data is stored and retrieved.”
"""